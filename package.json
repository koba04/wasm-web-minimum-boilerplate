{
  "name": "wasm-web-minimum-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:wasm && npm run build:bindgen",
    "build:wasm": "cargo build --target wasm32-unknown-unknown",
    "build:bindgen": "wasm-bindgen target/wasm32-unknown-unknown/debug/hello.wasm --out-dir ./public/wasm --target web",
    "clean": "rm -rf public/wasm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "koba04",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
